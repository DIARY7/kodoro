package com.kodoro.kodoro.repository;

import com.kodoro.kodoro.model.PrixUsuel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrixUsuelRepository extends JpaRepository<PrixUsuel,Integer> {
    
}
